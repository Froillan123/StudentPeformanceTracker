using System;
using System.Collections.Generic;
using System.Linq;

namespace StudentPeformanceTracker.Helpers
{
    public static class SeederHelpers
    {
        // TODO: Add helper methods when models are created
        // public static Course GetCourse(Courses courseEnum, Course[] courses) =>
        //     courses.First(c => c.CourseName == courseEnum);
        //
        // public static Subject GetSubject(string edpCode, Subject[] subjects) =>
        //     subjects.First(s => s.EDPCode == edpCode);
    }
}